# Bump this and push a matching git tag (e.g. v1.0.1) to ship an auto-update.
# The release workflow builds PetabyteAgent.exe and attaches it to the GitHub
# Release for that tag; running .exe installs find it via the Releases API.
VERSION = "1.0.0"
